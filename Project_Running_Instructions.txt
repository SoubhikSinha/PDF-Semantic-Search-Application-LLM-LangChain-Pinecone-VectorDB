1. Create env : conda create --prefix ./venv python==3.11 -y

2. Activate env : conda activate venv/

3. Install dependencies : pip install -r requirements.txt

4. Create and copy --> OpenAI API Key (https://platform.openai.com/api-keys) --> paste it to ".env" file

EXTRA INFO
=========
PDF Content ▶️ Text Chuncks ▶️ --> Vector Embeddings --> Vectors ▶️ Vector Search DB ▶️ Similarity Search (PineCone) 

5. Create and copy --> Pinecone Vector Index API Key (https://www.pinecone.io/) --> paste it to the desired location in the "test.ipynb" file

6. Run the notebook

7. (https://www.pinecone.io/) --> Check the index created --> see the vectors that were stored

8. you can even put your own query, condition it must be relevant to the document content